'use client';

/**
 * Re-export of the main ErrorBoundary component
 * This file is kept for backward compatibility with existing imports
 */
import ErrorBoundary from '@/components/ErrorBoundary';

export default ErrorBoundary; 